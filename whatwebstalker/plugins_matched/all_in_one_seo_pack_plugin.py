import sys
import os
			
class all_in_one_seo_pack_plugin:
    def get_rules(self):
        return(self.rules)
    def __init__(self):
        self.rules = 
        [
			{ "text" : '<!-- /all in one seo pack -->' }
			{ "version" : '/<!-- All in One SEO Pack ([\d\.]+) by Michael Torbert of Semper Fi Web Design/ }
		]

