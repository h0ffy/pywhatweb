import sys
import os
			
class envision_plugin:
    def get_rules(self):
        return(self.rules)
    def __init__(self):
        self.rules = 
        [
			{ "search" : 'headers[server]", "version" : '/^Content Interface Corp - enVision ([^\s]+)/ }
	]

