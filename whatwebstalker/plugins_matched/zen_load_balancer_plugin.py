import sys
import os
			
class zen_load_balancer_plugin:
    def get_rules(self):
        return(self.rules)
    def __init__(self):
        self.rules = 
        [
			{ "url" : '/config/global.conf", "version" : '/#version ZEN\s+\$version=\"([^\"]+)"/ }
			{ "search" : 'headers[www-authenticate]", "text" : 'Basic realm="Zen Load Balancer"' }
		]

