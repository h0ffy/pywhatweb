import sys
import os
			
class mikrotik_routeros_plugin:
    def get_rules(self):
        return(self.rules)
    def __init__(self):
        self.rules = 
        [
			{ "url" : '/webfig/iframe.html", "text" : '<body onload="parent.generateContent(parent.location.hash.substr(1));">' }
		]

