import sys
import os
			
class cacti_plugin:
    def get_rules(self):
        return(self.rules)
    def __init__(self):
        self.rules = 
        [
			{ "text" : '<title>Login to Cacti</title>' }
			{ "text" : '<body bgcolor="#FFFFFF" onload="document.login.login_username.focus()">' }
	]

