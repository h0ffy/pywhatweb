import sys
import os
			
class anygate_plugin:
    def get_rules(self):
        return(self.rules)
    def __init__(self):
        self.rules = 
        [
			{ "url" : '/index.asp", "text" : '<title>AnyGate' }
			{ "url" : '/index.asp", "text" : '사용자 암호가 설정되어 있지 않습니다.", "string" : 'No Password"}
		]

