import sys
import os
			
class minecraft_server_plugin:
    def get_rules(self):
        return(self.rules)
    def __init__(self):
        self.rules = 
        [
			{ "text" : '<span>Map type:</span><select id="mode" onchange="show(1,$(\'#mode\').val())">", "os" : 'MineOS (Linux)" }
			{ "text" : '<title>MineOS Admin Page</title>", "os" : 'MineOS (Linux)" }
	]

