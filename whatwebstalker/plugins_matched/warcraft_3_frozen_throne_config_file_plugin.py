import sys
import os
			
class warcraft_3_frozen_throne_config_file_plugin:
    def get_rules(self):
        return(self.rules)
    def __init__(self):
        self.rules = 
        [
			{ "text" : '// Radius to give XP to teammates near where the special objective is completed (rescued hosties", "bomb planted", "killed vip", "vip escaped", "default is 750)' }
	]

